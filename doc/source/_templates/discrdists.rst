{{ fullname }}
{{ underline }}

.. currentmodule:: {{ module }}

.. auto{{ objtype }}:: {{ objname }}

.. Generate stubs for all the methods of the distributions present in scipy.stats.

Methods
-------

.. autosummary::
   :toctree:

   {{ name }}.rvs
   {{ name }}.pmf
   {{ name }}.logpmf
   {{ name }}.cdf
   {{ name }}.logcdf
   {{ name }}.sf
   {{ name }}.logsf
   {{ name }}.ppf
   {{ name }}.isf
   {{ name }}.moment
   {{ name }}.stats
   {{ name }}.entropy
   {{ name }}.expect
   {{ name }}.median
   {{ name }}.mean
   {{ name }}.var
   {{ name }}.std
   {{ name }}.interval
   {{ name }}.__call__
